@model List<NoteAppMVCPattern.Models.AppUser>

@{
    // 💡 1. BU SAYFA İÇİN ÖZEL LAYOUT KULLAN
    Layout = "_AuthLayout";
    ViewData["Title"] = "Hoş Geldiniz";
}

@* <div class="background-videos-container">
    <video autoplay muted loop playsinline class="bg-video top-left-video">
        <source src="~/vids/space.mp4" type="video/mp4">
        Tarayıcınız video etiketini desteklemiyor.
    </video>
    <video autoplay muted loop playsinline class="bg-video bottom-right-video">
        <source src="~/vids/baloon.mp4" type="video/mp4">
        Tarayıcınız video etiketini desteklemiyor.
    </video>
    @* Diğer videolarınızı buraya ekleyebilirsiniz 
</div> *@

@* 2. TempData mesajları bazen Layout'ta olduğu için, burada tekrar partial çağırmaya gerek kalmayabilir *@
@await Html.PartialAsync("_TempDataMessage")


@* ... (Mevcut kodlarınız) ... *@

<div class="container-fluid">
    <div class="row align-items-center" style="min-height: 100vh;">

        @* <div class="col-md-6 text-center text-white">
            <h1 class="display-3 fw-bold">DÜZEN SENİN ZİHNİNDE BAŞLAR.</h1>
            <p class="lead mt-4 d-none d-md-block">Notlarını dijital evrende kaybetmeden, şeffaf bir zihinle yaşa.</p>
        </div> *@

        <div class="frosted-glass-container mx-auto" style="width: 500px; min-width: 300px;">

            <h2 class="mb-5 text-center">Welcome to the Note App</h2>

            <div class="d-grid gap-3 mx-auto" style="width: 70%;">

                <a asp-controller="User" asp-action="Register" class="btn btn-primary btn fw-bold w-80">Kayıt Ol</a>
                <a asp-controller="User" asp-action="Login" class="btn btn-success btn fw-bold w-80">Giriş Yap</a>

            </div>
        </div>

    </div>
</div>